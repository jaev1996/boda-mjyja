<div class="relative w-full min-h-screen flex flex-col items-center justify-center overflow-hidden" id="hero">
  <div class="absolute inset-0 z-0 bg-[#4a2e1a]"></div>

  <div class="relative z-10 w-[min(90vw,62vh)] aspect-[718/1080] rounded-2xl border-4 border-wedding-cream shadow-2xl ring-1 ring-wedding-accent/40 overflow-hidden">
    <img src={`${import.meta.env.BASE_URL}/images/pareja-boda.jpeg`} alt="María José y José Andrés" class="w-full h-full object-cover object-top" />

    <div class="absolute inset-x-0 bottom-0 h-2/3 bg-gradient-to-t from-black/70 via-black/25 to-transparent"></div>

    <div class="absolute inset-0 bg-[radial-gradient(ellipse_at_center,rgba(0,0,0,0.55),transparent_70%)]"></div>

    <div class="absolute inset-0 flex flex-col items-center justify-center text-center px-4">
      <div class="flex items-center justify-center mb-4">
        <div class="w-28 h-28 md:w-36 md:h-36 rounded-full border-2 border-wedding-accent/80 ring-1 ring-wedding-cream/50 flex items-center justify-center gap-1">
          <span class="font-serif text-6xl md:text-7xl text-wedding-accent italic drop-shadow-lg">J</span>
          <span class="font-serif text-5xl md:text-6xl text-wedding-cream drop-shadow-lg">M</span>
        </div>
      </div>
      <h1 class="font-serif text-5xl md:text-7xl text-wedding-cream font-semibold tracking-[0.15em] leading-tight drop-shadow-lg">JOSÉ ANDRÉS</h1>
      <span class="font-script text-2xl md:text-3xl text-wedding-accent my-1 drop-shadow-lg">y</span>
      <h1 class="font-serif text-5xl md:text-7xl text-wedding-cream font-semibold tracking-[0.15em] leading-tight drop-shadow-lg">MARÍA JOSÉ</h1>

      <div class="font-sans text-sm md:text-base tracking-[0.3em] text-wedding-cream/90 flex items-center justify-center gap-4 mt-6 drop-shadow-lg">
        <span class="flex flex-col items-center">
          <span class="font-sans text-xs uppercase tracking-wider opacity-70">Mié</span>
          <span class="font-serif text-3xl">11</span>
        </span>
        <span class="text-wedding-accent text-2xl">—</span>
        <span class="flex flex-col items-center">
          <span class="font-sans text-xs uppercase tracking-wider opacity-70">Dom</span>
          <span class="font-serif text-3xl">14</span>
        </span>
      </div>
      <p class="font-script text-4xl md:text-5xl text-wedding-accent mt-4 drop-shadow-lg">Isla de Margarita</p>
    </div>
  </div>

  <div class="absolute bottom-6 left-1/2 transform -translate-x-1/2 animate-bounce cursor-pointer z-30" id="scroll-indicator">
    <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="white" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg>
  </div>
</div>

<script>
  document.getElementById('scroll-indicator')?.addEventListener('click', () => {
    document.getElementById('save-weekend')?.scrollIntoView({ behavior: 'smooth' });
  });
</script>
