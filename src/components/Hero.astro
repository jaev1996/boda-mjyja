<div class="relative w-full min-h-screen flex flex-col items-center justify-center overflow-hidden" id="hero">
  <div class="absolute inset-0 z-0 bg-[#f5efe6]">
    <img src={`${import.meta.env.BASE_URL}/images/Fondo de agua.jpg`} alt="" class="absolute inset-0 w-full h-full object-cover opacity-45" />
  </div>

  <div class="relative z-10 flex flex-col items-center text-center px-4 py-16">
    <div class="monogram-oval mb-4 reveal">
      <img src={`${import.meta.env.BASE_URL}/images/palmera-jm.jpg`} alt="Monograma J&M" />
    </div>

    <p class="font-serif text-2xl md:text-3xl text-wedding-bronze tracking-[0.3em] mb-6 reveal">J + M</p>

    <h1 class="font-script text-5xl md:text-7xl text-wedding-bronze leading-tight drop-shadow-sm reveal">José Andrés Del Orbe</h1>
    <span class="font-script text-3xl md:text-4xl text-wedding-accent my-2 drop-shadow-sm reveal">&amp;</span>
    <h1 class="font-script text-5xl md:text-7xl text-wedding-bronze leading-tight drop-shadow-sm reveal">María José Carrero</h1>

    <p class="font-script text-2xl md:text-3xl text-wedding-bronze mt-8 tracking-wider drop-shadow-sm reveal">12 | 11 | 2026</p>
    <p class="font-script text-4xl md:text-5xl text-wedding-accent mt-3 drop-shadow-sm reveal">Isla de Margarita</p>
  </div>

  <div class="absolute bottom-6 left-1/2 transform -translate-x-1/2 animate-bounce cursor-pointer z-30" id="scroll-indicator">
    <svg width="32" height="32" viewBox="0 0 24 24" fill="none" stroke="#5C3A21" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M6 9l6 6 6-6"/></svg>
  </div>
</div>

<script>
  document.getElementById('scroll-indicator')?.addEventListener('click', () => {
    document.getElementById('save-weekend')?.scrollIntoView({ behavior: 'smooth' });
  });
</script>
